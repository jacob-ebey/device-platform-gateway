{
  "name": "device-platform-gateway",
  "version": "0.0.1",
  "description": "The code that runs on a gateway device",
  "main": "index.js",
  "scripts": {
    "test": "node node_modules/.bin/jest"
  },
  "author": "Jacob Ebey",
  "license": "ISC",
  "jest": {
    "verbose": true,
    "collectCoverageFrom": [
      "**/*.test.{js}",
      "!**/node_modules/**",
      "!**/vendor/**"
    ]
  },
  "dependencies": {
    "serialport": "^6.0.4"
  },
  "devDependencies": {
    "jest": "21.2.1"
  }
}
